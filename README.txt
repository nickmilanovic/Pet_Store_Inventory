Pet Store inventory tracker and filter used for a simulated pet store.
To run, first run index.js in the backend folder in one terminal.
Next, open a second terminal to run App.js, which is in the frontend folder.

Make sure to type npm install command in the terminal as node_modules is not included